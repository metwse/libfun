name: Generate Header-Only Library Action

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Generate the Header-Only Library
      run: make headeronly LIBFUN_H=/tmp/libfun.h

    - name: Deploy to the autogen/headeronly Branch
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'

        if git ls-remote --heads origin autogen/headeronly | grep -q 'refs/heads/autogen/headeronly'; then
          git fetch origin autogen/headeronly
          git checkout autogen/headeronly
        else
          git checkout --orphan autogen/headeronly
          git rm -rf .
        fi

        cp /tmp/libfun.h ./
        git add libfun.h

        if ! git diff --staged --quiet; then
          git commit -m 'auto-update header-only library'
          git push origin autogen/headeronly
        fi
